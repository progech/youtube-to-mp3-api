name: my-fastapi-app

build:
  # use pipenv to install dependencies
  command: pip install --system --deploy

run:
  # start the FastAPI application
  command: uvicorn main:app

environments:
  production:
    runtime: python3.8
